# RoboTwin 2.0 — SemTok P0 local smoke (short preflight, not a competence gate)
server:
  url: "ws://localhost:8000"
  timeout: 60

docker:
  image: ghcr.io/allenai/vla-evaluation-harness/robotwin:latest

output_dir: "./results"

benchmarks:
  - benchmark: "vla_eval.benchmarks.robotwin.benchmark:RoboTwinBenchmark"
    episodes_per_task: 1
    max_steps: 20
    params:
      task_name: pick_diverse_bottles
      task_config: demo_clean
      seed: 0
      instruction_type: seen
      test_num: 1
      skip_expert_check: true
    action_dim: 14
